## AAC Configuration File (Generated by AAC 5.0.4)
#
# How AAC works:
#   AAC's detection pipeline has three main stages: checks, database, and analysis
#    - Checks analyse player behaviour and record any interesting data available.
#      Some checks are additionally able to block cheats from being used. This is called mitigation.
#    - The database stores a record of aggregate information generated by all enabled checks for every player.
#      Old entries are automatically cleared when the server restarts.
#    - AAC periodically analyses each player's database entries, generating an overall risk score and a report.
#      If the generated risk score is greater than a configured threshold (see `yellow_score`, `red_score`), an alert
#      is shown. The detailed report can be viewed by clicking the alert, or by running /aac status <player>.
#
#   AAC takes many factors into account when generating its reports. These can be viewed, weighted, or disabled in
#   the `features` section of this file. Metadata about each feature can be viewed by hovering over the output in
#   /aac status <player>.
#
#   You can ask AAC to immediately generate a report for any player with /aac check <player>, and you can clear all
#   stored data for a player with /aac reset <player>. A list of all online players can be seen with /aac status.
#   Any colour coded player name sent in chat by AAC can be clicked for a detailed report.
#
# Silent mode:
#   Normally, AAC prevents certain cheats from working on your server, such as fly and nofall. If you do not want
#   AAC to block cheats at all, you can set the `mitigate` option in every check to `false`. In addition, if you do
#   not want to block nofall, you can set the `block_nofall` option to `false` in the move check.
#
#   You will still receive alerts from checks with mitigation disabled. If you want to disable a check entirely, you
#   can set the `enabled` option to false.
#
#   If you want to exempt any particular player from AAC entirely, you can use the /aac exempt <player> command.
#   This command persists through reboots and even between servers, if they are connected to the same database.
#
# Localisation:
#   By default, AAC never sends any messages to players. AAC's alerts include only the offending player's name and
#   hence do not need to be localised either. However, you may wish to translate the output of /aac status <player>
#   to make it easier for moderators to understand.
#   The text used for these is available in the `features` section of this configuration file.
#
# In this configuration file, all length units are blocks and all time units are seconds

# By default, AAC will use an SQLite database stored in its plugin data folder. MySQL support can be enabled here.
# All created tables have the prefix 'aac_'
mysql:
  enabled: false
  host: "localhost"
  port: 3306
  database: "aac"
  username: ""
  password: ""

# AAC supports cross server alerts through BungeeCord.
# AAC must be installed on every server on the network, and all instances must be connected to the same database.
# Do NOT enable this option if you are not using BungeeCord, or detections will be sent directly to players.
bungeecord: false

# Configure the different checks in AAC - modular components that try to detect certain categories of cheating
checks:
  # The delays check detects players that perform various actions faster than is normally possible
  delays:
    enabled: true

    # delays.vl represents an estimate of the total time advantage that a player has gained by cheating
    vl:
      mitigate: true
      threshold: 1.0
      decay: 0.01

    # Enable or disable delays checking for the following situations
    fast_break: true
    fast_place: true
    fast_use: true
    fast_bow: true
    regen: true
    fast_sneak: true
    fast_release: true
    break_delay: true

  # The move check detects a wide range of modifications relating to movement, including timer, nofall and velocity.
  move:
    enabled: true

    # move.vl represents an estimate of the total extra distance a player has travelled by cheating
    vl:
      mitigate: true
      threshold: 1.0
      decay: 0.01

    # move.timer represents an estimate of the total time advantage a player has gained by cheating
    timer: #CRASH EXPLOIT VULNERABLE!
      mitigate: false
      threshold: 1.0
      decay: 0.01

    # Phasing causes a player's vl to be increased more than normal violations
    # This should be set to a value >= vl.threshold to always block phase attempts
    phase_vl: 1.0

    # AAC can transparently block nofall by replacing ground calculations from the client with its own calculations
    block_nofall: true #Enabled by default

    # AAC will wait at most this long for a player to respond to velocity sent by the server
    max_vel_time: 0.5

    # This is how long AAC will remember piston extensions for
    piston_wait_time: 1.0

    # Enable/disable checking for noslowdown (sneak). Minor performance penalty if disabled
    check_sneak: true

    # Enable/disable checking for noslowdown (item use). Minor performance penalty if disabled
    check_item_use: true

    # These are thresholds that AAC uses in special cases where precise checking isn't possible
    flowing_speed: 0.2
    bubble_column_speed: 1.8
    bumping_speed: 0.1
    elytra_rocket_speed: 2.0
    riptide_speed: 4.2
    boat_hitbox_tolerance: 0.6
    shulker_hitbox_tolerance: 0.6
    elytra_landing_tolerance: 0.5

  # The aimbot check detects aim modifications used to gain an advantage in combat, including aimassists and killauras.
  aimbot:
    enabled: true

    check_sensitivity: true
    check_mouse_delta: true
    check_head_snap: true
    check_pitch_spread: true
    check_zero_point: true

  # The autoclicker check detects abnormal or irregular attack patterns characteristic of autoclickers and killauras.
  autoclicker:
    enabled: true

    check_tick_delay: true
    check_noswing: true
    check_timing: true
    sample_size: 20

  # The hitbox check verifies that players look at the entities they hit, and that those entities are in range.
  hitbox:
    enabled: true

    vl:
      mitigate: true
      threshold: 1.0
      decay: 0.01

    lag_compensation_ticks: 12
    hit_queue_size: 4
    check_blocks: true

  # The interact check looks for players performing impossible or abnormal interactions with the world.
  interact:
    enabled: true

    # AAC will accept interactions as long as the player is aiming close to their target block
    # This is the max angle that AAC will accept (in radians)
    max_angle_diff: 1.0

    # AAC will not check interactions with these materials
    exempt: []

    vl:
      mitigate: true
      threshold: 1.0
      decay: 0.01

  # The misc check looks for certain accurate indicators of cheating
  misc:
    enabled: true

    invalid_pitch: true
    player_abilities: true
    rotation_rate: true

# Using the features above, AAC will periodically analyse players on the server and generate a risk score.
# Analysis results can be accessed for all players at once with '/aac status', or detailed reports can be generated
# for individual players with /aac status <player> [players...]
analysis:
  # AAC analyses one player at a time, e.g. if your server has 50 players, they will each be analysed every 50 * period
  # seconds. You can force AAC to analyse any particular player at any time with /aac check <player> [players...]
  period: 1.0

  # How far back should AAC look when analysing a player?
  history: 1
  history_time_unit: DAYS # https://docs.oracle.com/javase/8/docs/api/java/util/concurrent/TimeUnit.html

  # Should AAC automatically clear database entries older than 'history' when the server starts?
  clear_stale_entries: true

  # At what score should a player's username be coloured yellow in /aac status?
  yellow_score: 100

  # At what score should a player's username be coloured red in /aac status?
  red_score: 200

# You can sort checks into your own categories here to automatically execute commands for them.
# The command list for the highest entry with a key <= report score is executed in order.
# Without PlaceholderAPI, you can only use the %player_name% placeholder. Otherwise, you can use any
# PlaceholderAPI placeholder that you have installed. If and only if you use PlaceholderAPI, you can
# download the Player extensions to use %player_name%: /papi ecloud download Player
actions:
  global:
    features:
      - aac_*
    thresholds:
      300:
        - "ncp log stream notify %player_name% ha usato cheat generici (heuristic)!"
        - "aac reset %player_name%"
        - "matrix kick %player_name% &5&l[&dAntiCheat&5&l] &e> &a&oCheats rilevati e bloccati!"
  combat:
    features:
      - aac_aimbot_*
      - aac_aimassist_*
      - aac_clk_*
      - aac_hitbox_*
    thresholds:
      200:
        - "ncp log stream notify %player_name% ha usato pattern incerti per il combattimento!"
        - "aac reset %player_name%"
        - "matrix kick %player_name% &5&l[&dAntiCheat&5&l] &e> &a&oNon puoi utilizzare cheats per il combattimento!"
  misc:
    features:
      - aac_misc_abilities
      - aac_misc_pitch
    thresholds:
      100:
        - "ncp log stream notify %player_name% ha utilizzato abilita insolite!"
        - "aac reset %player_name%"
        - "matrix kick %player_name% &5&l[&dAntiCheat&5&l] &e> &a&oAbbiamo rilevato abilita insolite nel tuo client!"
  movement:
    features:
      - aac_move_*
      - aac_delays_sneak
      - aac_delays_release
    thresholds:
      200:
        - "ncp log stream notify %player_name% ha inviato pacchetti di movimento insoliti!"
        - "aac reset %player_name%"
        - "matrix kick %player_name% &5&l[&dAntiCheat&5&l] &e> &a&oRilevati movimenti insoliti da te compiuti!"
  interact:
    features:
      - aac_interact_*
    thresholds:
      200:
        - "ncp log stream notify %player_name% ha interagito in maniera sospetta!"
        - "aac reset %player_name%"
        - "matrix kick %player_name% &5&l[&dAntiCheat&5&l] &e> &a&oInterazioni anomale rilevate!"
  delays:
    features:
      - aac_delays_*
    thresholds:
      200:
        - "ncp log stream notify %player_name% ha interagito troppo rapidamente con gli oggetti!"
        - "aac reset %player_name%"
        - "matrix kick %player_name% &5&l[&dAntiCheat&5&l] &e> &a&oHai interagito troppo rapidamente con le cose!"
  FallDamage:
    features:
      - aac_move_*
    thresholds:
      5:
        - "effect %player_name% instant_damage"
  FastBreak:
    features:
      - aac_delays_break
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta spaccando i blocchi molto rapidamente."
  FastPlace:
    features:
      - aac_delays_place
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta piazzando i blocchi molto rapidamente."
  FastUse:
    features:
      - aac_delays_consume
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta mangiando troppo velocemente."
  FastBow:
    features:
      - aac_delays_bow
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta frecciando troppo velocemente."
  FastRegen:
    features:
      - aac_delays_regen
    thresholds:
      20:
        - "ncp log stream notify %player_name% si sta rigenerando troppo velocemente."
  FastSneak:
    features:
      - aac_delays_sneak
    thresholds:
      20:
        - "ncp log stream notify %player_name% si sta muovendo accovacciato rapidamente."
  FastInventory:
    features:
      - aac_delays_release
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta droppando oggetti molto rapidamente."
  FastInteract:
    features:
      - aac_delays_breakdelay
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta interagendo molto rapidamente."
  Nofall:
    features:
      - aac_move_nofall
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta provando ad eludere il danno da caduta."
        - "effect %player_name% instant_damage"
  NoVelocity:
    features:
      - aac_move_vel
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta eludendo il knockback del server."
        - "effect %player_name% instant_damage"
  ElytraModify:
    features:
      - aac_move_elytra
    thresholds:
      20:
        - "ncp log stream notify %player_name% si sta muovendo in maniera anomala con Elytra."
        - "effect %player_name% instant_damage"
  VehicleInteract:
    features:
      - aac_move_vehicle
    thresholds:
      20:
        - "ncp log stream notify %player_name% si sta spostando in maniera errata col veicolo."
        - "effect %player_name% instant_damage"
  WaterWalk:
    features:
      - aac_move_water
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta provando a camminare sulle acque."
        - "effect %player_name% instant_damage"
  LavaWalk:
    features:
      - aac_move_lava
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta provando a nuotare sotto la lava."
        - "effect %player_name% instant_damage"
  Noslowdown:
    features:
      - aac_move_noslow
    thresholds:
      20:
        - "ncp log stream notify %player_name% si sta muovendo rapidamente mentre mantiene oggetti pesanti."
        - "effect %player_name% instant_damage"
  Genmove:
    features:
      - aac_move_generic
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta compiendo movimenti impossibili."
        - "effect %player_name% instant_damage"
  Timer:
    features:
      - aac_move_timer
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta provando a modificare il suo tempo di gioco."
        - "effect %player_name% instant_damage"
  MoveInfo:
    features:
      - aac_move_inf
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta inviando informazioni invalide di movimento."
        - "effect %player_name% instant_damage"
  Aimbot:
    features:
      - aac_aimbot_*
      - aac_aimassist_*
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta mirando con la testa in maniera troppo strana."
  Forcefield:
    features:
      - aac_clk_*
      - hitbox_*
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta avendo una combinazione di click e mira troppo insolita."
  ForceBreak:
    features:
      - aac_interact_break
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta spaccando blocchi fuori dal suo campo visivo."
  ForcePlace:
    features:
      - aac_interact_place
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta piazzando blocchi fuori dal suo campo visivo."
  IllegalInteract:
    features:
      - aac_interact_generic
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta interagendo in maniera anomala coi blocchi."
  Derp:
    features:
      - aac_misc_*
    thresholds:
      20:
        - "ncp log stream notify %player_name% sta ruotando il capo in maniera insolita."

# AAC compiles data from all of its checks and uses it to generate a set of weighted features for each player
# Each feature accepts certain placeholders. A list of valid placeholders can be found by hovering over each feature's
# tooltip. You can set the weight of any feature to 0 to disable it.
features:
  aac_delays_break:
    weight: 1.5
    format: "I blocchi vengono rotti dal giocatore più velocemente di quanto sia normalmente possibile!"
  aac_delays_place:
    weight: 1.5
    format: "I blocchi vengono posizionati dal giocatore più velocemente di quanto sia normalmente possibile!"
  aac_delays_consume:
    weight: 1.5
    format: "Gli oggetti vengono consumati dal giocatore più velocemente di quanto sia normalmente possibile!"
  aac_delays_bow:
    weight: 1.5
    format: "Il giocatore tende l'arco più velocemente di quanto sia normalmente possibile!"
  aac_delays_regen:
    weight: 1.5
    format: "Il giocatore sta rigenerando la salute più velocemente di quanto sia normalmente possibile!"
  aac_delays_sneak:
    weight: 1.5
    format: "Il giocatore sta passando di nascosto più velocemente di quanto sia normalmente possibile!"
  aac_delays_release:
    weight: 1.5
    format: "Il giocatore sta rilasciando l'oggetto trattenuto più velocemente di quanto sia normalmente possibile!"
  aac_delays_breakdelay:
    weight: 1.5
    format: "Il ritardo tra le azioni di blocco del giocatore è più breve di quanto sia normalmente possibile!"
  aac_move_nofall:
    weight: 1.5
    format: "Il giocatore sta cercando di evitare danni da caduta!"
  aac_move_vel:
    weight: 1.5
    format: "Il giocatore ignora la velocità inviata dal server!"
  aac_move_elytra:
    weight: 1.5
    format: "Il giocatore si è spostato {rate} blocchi incorretti con l'elytra!"
  aac_move_vehicle:
    weight: 1.5
    format: "Il giocatore si è spostato spesso in modo errato in un veicolo!"
  aac_move_water:
    weight: 1.5
    format: "Il giocatore ha spostato {rate} blocchi in modo errato nell'acqua!"
  aac_move_lava:
    weight: 1.5
    format: "Il giocatore ha spostato {rate} blocchi in modo errato in lava!"
  aac_move_noslow:
    weight: 3.5
    format: "Il giocatore ha spesso ignorato i rallentamenti furtivi/oggetti!"
  aac_move_generic:
    weight: 1.5
    format: "Il giocatore ha spostato {rate} blocchi in modo errato!"
  aac_move_timer:
    weight: 1.5
    format: "Il giocatore sta cercando di accelerare il tempo!"
  aac_move_inf:
    weight: 1.5
    format: "Il giocatore sta inviando pacchetti di movimento non validi!"
  aac_aimbot_spread:
    weight: 2.0
    format: "Il campo del giocatore è variato in modo insolito durante il combattimento!"
  aac_aimbot_sync:
    weight: 2.0
    format: "Gli schemi di mira orizzontale e verticale del giocatore non sono sincronizzati!"
  aac_aimbot_horz:
    weight: 2.0
    format: "Il giocatore ha mostrato schemi di imbardata caratteristici dell'aimbot!"
  aac_aimbot_vert:
    weight: 2.0
    format: "Il giocatore ha mostrato schemi di lancio caratteristici dell'aimbot!"
  aac_aimbot_snap:
    weight: 2.0
    format: "Il giocatore esegue schiocchi di testa anormali durante il combattimento!"
  aac_aimbot_zero:
    weight: 2.0
    format: "Gli schemi di mira del giocatore cambiano quando entrano in combattimento!"
  aac_aimassist_horz:
    weight: 2.0
    format: "Il giocatore ha mostrato schemi di imbardata caratteristici di aimassist!"
  aac_aimassist_vert:
    weight: 2.0
    format: "Il giocatore ha mostrato schemi di pitch caratteristici di aimassist!"
  aac_clk_noswing:
    weight: 1.5
    format: "Il giocatore non fa oscillare il braccio durante l'attacco!"
  aac_clk_tick_align:
    weight: 1.5
    format: "Le animazioni del braccio del giocatore sono allineate con i limiti del tick!"
  aac_clk_timing:
    weight: 1.5
    format: "I tempi dei pacchetti di attacco del giocatore sono caratteristici di killaura!"
  aac_clk_cps:
    weight: 1.5
    format: "Il giocatore fa clic a una media di {mean} cps durante il combattimento!"
  aac_clk_var:
    weight: 1.5
    format: "Il ritardo del giocatore tra i clic successivi ha una varianza di {mean}!"
  aac_hitbox_multi:
    weight: 1.5
    format: "Il giocatore ha attaccato più volte con un solo tick!"
  aac_hitbox_miss:
    weight: 1.5
    format: "Il giocatore attacca le entità senza guardarle!"
  aac_hitbox_walls:
    weight: 1.5
    format: "Il giocatore attacca le entità attraverso muri solidi!"
  aac_hitbox_reach:
    weight: 1.5
    format: "Il giocatore attacca i blocchi {value} più in là del normale!"
  aac_interact_generic:
    weight: 1.5
    format: "Il giocatore interagisce in modo sospetto con i blocchi!"
  aac_interact_break:
    weight: 1.5
    format: "Il giocatore rompe i blocchi fuori dalla sua linea di vista!"
  aac_interact_place:
    weight: 1.5
    format: "Il giocatore posiziona i blocchi fuori dalla sua linea di vista!"
  aac_misc_abilities:
    weight: 1.5
    format: "Il giocatore ha inviato pacchetti di abilità del giocatore non validi!"
  aac_misc_pitch:
    weight: 1.5
    format: "Il lettore ha inviato pacchetti di pitch di rotazione non validi!"
  aac_misc_rotation:
    weight: 1.5
    format: "Il lettore sta ruotando troppo velocemente!"
  aac_new_user:
    weight: 1.5
    format: "Il giocatore si è recentemente unito per la prima volta!"
  aac_new_addr:
    weight: 1.5
    format: "Il giocatore si è recentemente unito da un nuovo indirizzo IP!"
  aac_info_move:
    weight: 1.5
    format: "Dati insufficienti per analizzare il movimento del giocatore!"
  aac_info_combat:
    weight: 1.5
    format: "Dati insufficienti per analizzare il combattimento del giocatore!"
  aac_info_interact:
    weight: 1.5
    format: "Dati insufficienti per analizzare le interazioni del giocatore!"

# AAC will run the following commands from the CONSOLE before teleporting a staff member to a player
pre_spectate:
# - "aac sudo %player_name% vanish on" # /aac sudo <player> <command> executes <command> as <player>
  - "gamemode spectator %player_name%"
# AAC will run the following commands from the CONSOLE after teleporting a staff member back
post_spectate:
# - "aac sudo %player_name% vanish off"
  - "gamemode survival %player_name%"

# Can be any valid minecraft colour code, e.g. a corresponds to green
aac_prefix_colour: a
