#Configure how the exploit is detected
channels: ["MC|BEdit", "MC|BSign"]
max-size: 300

#Commands to dispatch when the exploit is detected.
#%player% will be replaced with the player name.
commands:
  - 'matrix kick %player% Crash exploit rilevato'
